The agent/internal/config package loads all runtime configuration from a .env file (optional), environment variables, CLI flags, and three JSON files under config/ (sources.json, scoring.json, keywords.json). It produces a single explicit Config struct with derived paths, validated provider credentials, and tuned scoring/keyword parameters. No global config exists; the root command passes the Config down through the call chain.
