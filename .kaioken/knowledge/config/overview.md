Three JSON configuration files control the Airfoil pipeline: keywords.json defines lexical signals for builder relevance, hype detection, and topic tagging; scoring.json parameterizes clustering, ranking weights, tier multipliers, recency decay, caps, and retention windows; sources.json declares 17 ingest sources across 5 tiers (labs, research, tools, press, community) with type-specific fetch options.
