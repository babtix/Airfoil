Story IDs use kebab-case prefix 's{N}-' followed by slugified title (e.g., s1-deepseek-r1); Scores are integers 0-100; tiers are 'major' (>=80) or 'notable' (<80) — observed threshold at 80; builder_relevant boolean gates inclusion in builder-focused feeds; false for policy/governance stories; Sources typed as 'lab' (primary), 'community' (HN, Reddit), 'press' (TechCrunch, Verge); tier 1=primary, 2=secondary, 4=press, 5=community; Dates in ISO8601 UTC (generated_at, date, ts); cluster_size integer reflects source article grouping; Body and takeaways are string arrays, not markdown — rendering layer handles formatting; state.json cursors and seen_urls keyed by source identifier for incremental fetch deduplication
