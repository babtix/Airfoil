Repository root holds top-level configuration templates (.env.example), ignore rules (.gitignore), and the canonical agent documentation (AGENTS.md) for Airfoil — a static AI-news site generated by a local Go pipeline (ingest → normalize → embed → cluster → score → summarize → write .md → commit → push) with a React+Vite frontend. No database or server; Git is the storage layer.
